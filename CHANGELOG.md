# NowTree 更新日志

记录每个版本的关键改动、修复与已知限制。

---

## 1.1.0（2026-08-09）

本版本新增 Habits 视图（主功能），将启动弹窗标题改为按时段问候，并修复关闭程序时弹窗层级被遮挡的问题。原计划的 1.0.4（按时钟过滤 / 跨边界重弹 / 每日励志语 / 导航天空格激活）一并纳入本版本。

### 新功能：Habits 视图
- 侧边栏新增「Habits」入口，与普通四类视图并列；habit 默认只在 Habits 视图显示，不进 Next，避免破坏 GTD 主线。
- 沿用现有 `Transaction` 单表，仅靠 `category='habit'` 区分，后端零新增结构；Habit ↔ 普通事务互转只需改 `category`（拖到其它视图即转成普通事务，反之拖进 Habits 即变成 habit）。
- 完成交互复用「打勾 + 置底灰显」；底层逻辑不同——habit 完成**次日 6 点自动复位**回未完成（启动补一次 + 每日 6:00 定时器，跨过 6:00 即把昨天完成的清零）。
- 视图内可拖拽排序（顺序存 `order_index`）；支持软删除进回收站（可恢复）；排除「一键清理」（habit 每天重置，无"已完成待清"语义）。
- 编辑 / 新增习惯隐藏优先级与时间要求（habits 聚焦「每天坚持」本身、不需要这些字段）；但**开放「一次性提醒」字段**（与全局一致，弹过即焚，故对 habit 只响一次）。转成 habit 时自动把 priority 清零为默认 1，消除「拖入项残留旧优先级 / 新建项=1」的数据不一致。每日 6 点重置时**顺带清掉跨天（昨天及更早）的 habit 提醒**，避免次日打开 app 补弹「昨天的习惯」——第二天是新的习惯周期；当天的迟到/未来提醒保留，仍可正常响一次。
- 侧边栏「Habits」支持数字键 `6` 快速切换（与 Inbox 1 / Next 2 / Project 3 / Waiting 4 / Someday 5 一致）。

### 新功能：启动弹窗按时钟过滤
- 按当前钟点映射时段：早 6:00–12:59 / 午 13:00–17:59 / 晚 18:00–23:59；深夜 0:00–5:59 显示「早点休息」提示、**不列任务**。
- 弹窗只展示**当前时段**的任务（不再平铺早/午/晚三栏）；未分配时段（time_slot = none）的任务不进入弹窗。
- 时段内任务按 `order_index` 排序，即你在列表里拖拽出来的顺序，清晰对齐你的安排。

### 新功能：跨边界自动重弹
- 在 6 / 13 / 18 / 24(=0) 点经过时，自动重新弹出弹窗并显示**对应时段**任务（与启动弹窗同一弹窗，非叠加）。
- 若弹窗保持打开跨过边界，内容会每分钟刷新、自动切到新时段。

### 新功能：每日励志语
- 弹窗顶部新增一句励志语，按「年内第几天」取模，**每天稳定一条**（同一天多次打开看到同一条），内置语录库、不联网。
- 想增删语录：编辑 `src/data/quotes.ts` 的 `QUOTES` 数组即可，无需改动其他代码。

### 优化：导航项支持空格键激活
- 用 Tab 将焦点移到侧边栏视图项后，按空格即可切换视图（与按钮行为一致），补全此前仅在按钮上生效、视图导航缺失的键盘激活。
- 回车键行为不变（仍触发全局「快速添加」）；焦点环等视觉样式保持不变。

### 优化：启动弹窗标题按时段问候
- 弹窗标题由固定的「今天」改为按当前时段显示问候语：早 6:00–12:59「早上好」/ 午 13:00–17:59「下午好」/ 晚 18:00–23:59「晚上好」/ 深夜 0:00–5:59「晚安」，与四个时段窗口一一对应。

### 优化：关闭确认弹窗始终置顶
- 点击 X 关闭程序时，先收起当前已打开的覆盖层（每日启动弹窗 / 设置 / 数据管理 / 回收站 / 快捷键 / 主题），再弹出「最小化到托盘还是退出」确认框，确保关闭确认框始终在最上层、不被其它弹窗遮挡。
- 同时收起数据管理弹窗内的应用内二级确认（清空数据确认 / 导入确认），避免极端场景下两个确认框叠在一起。
- 系统原生文件选择框（导出 / 导入时调用）由操作系统接管、应用层无法干预，且弹出时窗口已阻塞等待选择、本就点不到 X，故不在收起范围。
- 关闭确认展示期间拦截跨边界重弹定时器，不会重新弹出每日弹窗把确认框顶掉。

### 修复：Habits 相关
- **06:00 重置阈值算错（C1）**：`resetHabits` 原阈值恒为「今天 06:00」，导致凌晨（<6 点）打开 app 会把昨晚完成的 habit 提前清空。改为取「最近的 06:00 边界」——`now < 6 点` 时回退到昨天 06:00，确保 0-6 点仍显示已完成、到 6 点才重置。
- **转 habit 后残留进 Next（A1）**：把已被 1.0.2 自动晋升进 Next 的 waiting（show_in_next=1）拖进 Habits 时，原只改 category、show_in_next 残留，使其仍出现在 Next。转成 habit 时一并清空 show_in_next + time_slot + parent_id，符合「habit 不进 Next」。
- **已完成 habit 不重置（C2）**：completed_time 为空的已完成 habit（导入/历史数据）原不会被重置、卡在 completed；现 null 视为最旧、必命中重置。

### 工程
- 版本号四处（package.json / tauri.conf.json / Cargo.toml / App.tsx）升至 `1.1.0`。
- 新增 `src/utils/clock.ts`（钟点→时段映射）、`src/data/quotes.ts`（语录库）；`StartupModal` 重构为按时钟渲染。

---

## 1.0.3（2026-08-07）

本版本修复一项交互缺陷：在弹窗内（如编辑窗标题输入框）用鼠标拖选文本、拖到窗口外松手时，弹窗会被误关闭。

### 修复：拖选文本误触关闭弹窗
- **根因**：通用弹窗 `Modal` 的遮罩用 `onClick={onClose}` 关闭。在窗口内按下鼠标、拖到遮罩上松开，浏览器会把 `click` 派发到遮罩（mousedown 与 mouseup 的共同祖先）→ `onClose` 触发，弹窗关闭。
- **修法（纯前端，1 行级改动）**：遮罩改监听 `onMouseDown`，且仅当 `e.target === e.currentTarget`（即「直接按在遮罩本身」）时才关闭。从窗口内发起的拖选，其 mousedown 不在遮罩上，故不会误关；点击遮罩空白处关闭的行为保持不变。所有复用 `Modal` 的弹窗（新建 / 编辑 / 转换 / 整理 / 回收站等）一并受益。
- **范围**：不涉及数据库结构，无需迁移；重打包 exe 后生效。

### 新功能：一键清理提示音
- **行为**：各视图工具栏「一键清理」二次确认后真正删除已完成事务时，播放一段轻提示音。
- **实现**：新增 `src/utils/clearSound.ts`（`playClearSound()`），在 `useListActions.cleanCompleted()` 的删除分支调用；音效文件置于 `src/assets/audio/clear.wav`（首次打包前请放入该文件，文件名需一致）。
- **共用开关**：复用勾选音（`checkbox-pop.wav`）的全局音效开关（同一 localStorage key），设置里一个开关同时控制两个音效的开关；开关关闭时静默。
- **范围**：纯前端，不涉及数据库结构。

### 工程
- 版本号四处（package.json / tauri.conf.json / Cargo.toml / App.tsx）升至 `1.0.3`。

## 1.0.2（2026-08-03）

本版本修复一项"具体日期到期自动变今日"的触发缺陷，并对应升级版本号。

### 修复：具体日期到期自动变「今日」不触发（B 场景）
- **根因**：原逻辑在 app 挂载瞬间即扫描本地事务做 deadline 归一，但本地数据由 `loadActive()` 异步加载，挂载那一刻数据尚未到位 → 扫描为空、几乎永不生效；唯一能触发的"每日 0 点定时扫描"又要求 app 常开跨过午夜，正常早晨打开的用户感知为"功能消失"。
- **修法（方案 A，最小改动）**：① 删除挂载瞬间的立即扫描，仅保留「每日 0 点」定时扫描；② 在 `store.loadActive()` 把数据写入 `active` 之后补调一次 `normalizeDeadlines()`，使本地数据加载完成即刻归一。
- **效果**：早晨打开 app → 数据就绪 → 立即把"具体日期 = 今天"的任务转为「今日」；跨天仍由 0 点定时器兜底。行为与 1.0.1 的截止日锚点完全兼容，重复扫描幂等无害。

### 新功能：Waiting for 到期自动进 Next
- **行为**：Waiting for 中设了时间要求（截止日）的事务，当系统日期到达该时间要求那天（含逾期），自动翻 `show_in_next=1` 进入 Next Actions；到达后持续停留直到你手动处理或改类别/清截止日。
- **范围**：四种截止类型（具体日期 / 今日 / 本周 / 本月）全部认——它们的锚点日期都已在保存时写入 `deadline_date`（本周→周日、本月→月底、今日→当天），晋升判定统一比较 `deadline_date <= 今天`，不依赖类型分支，故四种都不会漏。
- **防骚扰（一次性）**：新增 `wait_auto_next` 标记列（默认 0），自动晋升后置 1；之后用户手动移出 Next 不再自动复加，规避每日 0 点扫描的死循环。
- **来源标签复用**：晋升项在 Next 中按现有逻辑显示"Waiting for"来源标签，无需新增 UI；不弹桌面提醒（避免累积爆弹）。
- **挂载点**：在 `normalizeDeadlines()` 之后同一次扫描执行（`loadActive` 写入后 + 每日 0 点），复用现有触发时机，无需新定时器。

### 优化：编辑逾期项不再"救活"过期时间要求
- **诉求**：打开一条已逾期的事务（如昨天选了「本日」），编辑窗里时间要求仍显示那个过期的「本日」，且保存时若只改了别的字段，截止日会被重算成"今天" → 逾期红标凭空消失，丢失"已过期"信号。
- **修法（纯前端）**：
  1. 编辑窗对**已逾期项**把时间要求选择器「伪装」显示为「无」，但内部 `deadlineType` 仍是真实值，**不触碰数据** → 列表中的逾期红标照常保留；用户一旦操作该栏即解除伪装、走真实逻辑。
  2. 新增 `deadlineInteracted` 标记：保存时若用户**没动过时间要求那栏**，直接原样回写原始 `deadline_type` / `deadline_date`，跳过 `resolveDeadline` 重算 → 打开看一眼改个备注也不会救活逾期。
  3. 「时间要求早于今天」的 toast 拦截同样以 `deadlineInteracted` 把关，只对用户**主动设置的过去日期**生效，不再误拦"只改备注"。
- **范围**：所有类别通用（新建场景无逾期项，伪装不触发，行为不变）；不升版本、不改数据库结构。

### 工程
- 版本号四处（package.json / tauri.conf.json / Cargo.toml / App.tsx）升至 `1.0.2`。

## 1.0.1（2026-08-02）

本版本聚焦"正确性修复 + 相对截止日逻辑回归 + 代码整理"，版本号保持 1.0.1。

### 正确性修复（5 项）
- **写操作不再静默失败（A1）**：`useTxStore` 的 `addInbox` / `removeInbox` / `convertInbox` / `addChild` / `createTx` 统一包 `try/catch` → `set({error})`，写入失败会在 UI 暴露而非吞掉。
- **提醒不再死循环重弹（A2）**：`checkReminders` 改为"先置 `reminder_done=1` 再发通知"，避免同一条到期提醒反复弹窗。
- **内存回退模式也能刷新（A3）**：`memoryRepo.update` / `convertFromInbox` 返回全新对象而非原地改同引用，浏览器（非 Tauri）回退模式列表也能正确重渲染。
- **拖拽排序不再只写一半索引（A4）**：`reorder` 由 `Promise.all` 改为顺序 `await` + `try/catch`，批量拖动后索引完整一致。
- **数据路径不再硬编码（C11）**：`App.tsx` 用 Tauri `appDataDir()` + `join()` 在运行时算真实 SQLite 路径，替换 `C:\Users\你\...` 占位符；非 Tauri 环境给出明确回退提示。

### 相对截止日锚定（修复"今日却默认逾期"）
把「今日 / 本周 / 本月」与「具体日期」统一抽象为"都解出一个确定的截止日（deadline_date）"：
- `today` → 当天日期；`week` → 那周周日；`month` → 那月最后一天；`date` → 所选日期。
- **锚点 = 你"修改时间要求那一刻"的日期**：重新选一次即刷新锚点，按"你承诺的那一周/那月"算逾期，符合直觉。旧任务（`deadline_date` 为空）仍按创建时刻算，零回归。
- 修复此前"昨日创建、今天设今日却直接标红逾期"的困惑；新增 5 个锚点行为单元测试（`vitest` 共 42 passed）。

### 代码整理（不改变功能）
- **B2+E 统一拖拽引擎**：把 NextView 与列表拖拽重复的"拖拽视觉接管（光标/禁选）+ 阈值判定"抽到 `dragUtils` 共享（`enterDragVisuals` / `exitDragVisuals` / `isBeyondThreshold`）；落点业务编排与 NextView 的三个时间槽表等独有逻辑一行未动，规避拖拽回归。
- **C9 拆分 App.tsx 上帝组件**：新建 `useShell` hook，把窗口关闭拦截、开机自启、勾选音、导入导出、重置确认、快捷键监听等外壳副作用整体收口，App.tsx 由 ~533 行瘦到 ~290 行；grep 确认 App 内已无直接 `invoke(` 调用。

### 工程
- 版本号四处（package.json / tauri.conf.json / Cargo.toml / App.tsx）保持 `1.0.1`。
- 测试：`tsc --noEmit` 0 错；`vitest run` 42 passed（原 37 + 新增 5 个截止日锚点用例）。

### 已知限制（与 1.0.0 一致）
- 提醒依赖 NowTree 正在运行（托盘态进程存活仍会提醒；彻底退出后不提醒）。
- 安装包产物：`build-release.bat` 同时输出可直接双击的 `NowTree.exe`、WiX `.msi`（`NowTree_1.0.1_x64_en-US.msi`）与 NSIS `setup.exe`（`NowTree_1.0.1_x64-setup.exe`）。

---

## 1.0.0（2026-07-25）

首个稳定版。在 0.2.0 打磨基础上，针对回归清单修复若干体验问题，并准备发布。

### 交互修复（来自 1.0 前回归）
- **导航焦点残留**：鼠标选视图后按数字键切换，原视图不再残留白/绿焦点框（数字切换时主动失焦）。
- **跨栏拖拽自动滚动**：从「未分配时段」拖入右侧「早/午/晚」展开时段时，右栏自身会滚动并高亮边缘行（滚动容器改为取光标所在列表）。
- **回收站恢复 SQL**：修正 `restore_transaction` 的递归 CTE，恢复任意节点只向上拉回「自身 + 所有祖先」，不再误引用不存在的列；恢复子事务会连带把父项目一起拉回，绝不产生孤儿。
- **回收站选择性恢复**：恢复父项目时不再自动拉回其子孙，子孙继续留在回收站，由你自行逐个恢复。
- **回收站标签**：带父的子事务在回收站统一显示为「Project 子事务」，不再误显为独立「Next Action」。
- **Inbox 删除确认态**：点「删除」弹出二次确认后，若转去点「编辑 / 整理 / 新增 / 加入 Next / 批量转换 / 进入多选」等其它入口，确认态自动取消（点别处即取消）。

### 数据 / 工程
- **导出导入增强**：导出 JSON 增加顶层 `exported_at` 字段（真实备份时间，与每条记录分离）；默认文件名带日期 `nowtree-backup-YYYY-MM-DD.json`；导入改为两步（选文件 → 显示备份日期与条数确认 → 真正导入）；导入/导出完成后数据管理窗口自动关闭；成功提示改为「成功导入 N 条事务」。
- 版本号四处（package.json / tauri.conf.json / Cargo.toml / App.tsx）升至 1.0.0。

### 已知限制（与 0.2.0 一致）
- 提醒依赖 NowTree 正在运行（托盘态进程存活仍会提醒；彻底退出后不提醒）。
- 安装包产物：`build-release.bat` 同时输出可直接双击的 `NowTree.exe`、WiX `.msi`（`NowTree_1.0.0_x64_en-US.msi`）与 NSIS `setup.exe`（`NowTree_1.0.0_x64-setup.exe`，需本机已安装 WiX v3 与 NSIS）。

---

## 0.2.0（2026-07-23）

### UI / 交互打磨（0.1.20 之后的体验修复汇总）
- **弹窗统一居中**：所有弹窗（回收站 / 快捷键 / 主题 / 数据管理 / 编辑 / 新增 / 整理）由顶部对齐改为垂直居中，并用 `margin:auto` 保护——弹窗超高时自动贴顶、可完整滚动，避免顶部被裁切。
- **正文字号收敛**：正文 5 档（11/11.5/12/13/14）收敛为 3 档（12/14/16），层级更克制统一（原映射已存档，可回退）。
- **菜单栏弹窗化**：「主题」「数据管理」改为独立弹窗（主题卡片三选、数据备份与恢复）；保留开机自启动与版本 / motto 不动。
- **快捷键说明文案**：1–5 加"视图"二字；删去"灵感收集"标注；Esc 改为"关闭当前窗口"、Enter 改为"快速新增 / 快速保存"；删去"长按拖拽"条。

### 行为修复
- **回收站空态**：回收站为空时，「清空回收站」按钮禁用（变灰 + 禁止光标 + 悬停提示"回收站为空，无需清空"），不再弹确认。
- **一键清理空态**：「一键清理」按钮在"无已完成事务"时复用同一禁用方案（变灰 + 禁止光标 + 提示"没有已完成的事务，无需清理"）。
- **灵感（Inbox）编辑**：修复备注清空不生效（Rust 端加 `clear_note` 标志，与 `clear_reminder` 同范式）；编辑窗标题区分"编辑灵感 / 编辑事务"。
- **Next 中展示项的类型模块**：Project 子项 / Waiting / Someday 凭 `show_in_next` 透出 Next 时，编辑窗统一隐藏「类型」模块（归属由"来源"徽标说明），不再误改类别导致错乱。
- **编辑 / 新增弹窗回车冲突**：弹窗内回车不再穿透到全局 FAB 快捷键（弹窗容器 `stopPropagation`），且保存按钮显式支持回车保存；保存后焦点回落到按钮时的默认点击被拦截，杜绝"回车连锁弹新增"。

### 数据层（Rust / SQLite）
- 父事务删除 / 恢复 / 彻底删除 / 清空回收站：统一递归级联整棵子树（向下后代 + 向上祖先），修复旧 SQL 因行继续符吞空格导致的 `1UNION` 解析报错。
- `update_transaction` 支持 `clear_note` 标志，解决 JSON patch 下无法区分"清空"与"未传"的可空字段坑。

### 工程
- 版本号 0.1.20 → 0.2.0（源码四处 + Cargo / npm 锁文件同步；`target/`、`dist/` 为构建产物，重编自动更新）。

---

## 0.1.20（2026-07-22）

### 工程化收尾（地基加固，无功能变化）
- **sync_id（UUID）+ deleted_at 全链路**：每条事务现在有稳定全局唯一 ID 与软删时间戳，为将来手机端收集 / 云同步铺路（现在加最便宜，将来再改要迁移老数据更痛）。
- **service 层抽取**：类别迁移规则、`canNext`、来源文案等纯业务逻辑从各视图收口到 `src/services/transactionService.ts`，UI 只调不写规则。
- **共享 dragUtils 底层原语**：拖拽的「长按阈值 / 可滚动祖先 / 最近行回退 / 自动滚动」抽成 `src/hooks/dragUtils.ts`，`useListDrag` 与 `NextView` 共用，消除复制粘贴式重复（两套编排各自保留，行为不变）。
- **vitest 单元测试**：新增 26 个用例（类型 / 业务规则 / 拖拽几何），`npm run test` 全绿，防回归。
- **Rust 后端标题校验**：`create_transaction` / `update_transaction` 拒绝空标题（去空格后）与超长（>200 字符），前端之外再加一道门禁。

### 修复
- **Inbox 标题前空白占位符**：非多选模式下 `tx-gutter` 不再渲染，灵感标题前不再留 18px 空白。

## 0.1.19（2026-07-21）

### 修复（拖拽规则对齐用户逻辑）
- **Next 中外来项拖到类别导航不再高亮**：凭 `show_in_next` 展示在 Next 的 waiting/someday/project 项，拖动时左侧类别导航不再显示可投放高亮（实际也无法改类别，避免误导）；它们仍可排序、分配时段。
- **Someday / Waiting 改类别保持 Next 展示**：0.1.18 误把 `show_in_next`、`time_slot` 一并清零，现已改为「仅改 category」，留在 Next 的展示状态（位置）完全保留。
- **Project 子事务改类别保持 Next 展示**：同上修正——子事务跨类别拖动后不再清 `show_in_next` / `time_slot`，「加入 Next」的展示状态保持不变。
- **Project 父项可正常排序**：修复「全部子事务完成」的父项因 `disabled: parentDone` 被整行禁拖、连排序都做不了的隐藏 bug；现仅真正 `status=completed` 的父项禁拖（与「父拖动排序」规则一致）。

### 新增（已完成长按提示 + 自动滚动回归）
- **已完成事务长按提示**：在任意列表长按已完成项不再「毫无反应」，而是弹出轻量 toast「已完成事务无法拖拽」（复用 App 底部 toast 通道，新建 `src/toast.ts` 全局事件）。
- **自动滚动补回所有视图**：0.1.18 重写 `useListDrag` 时丢了边缘自动滚动，现重新接入（拖到列表上下边缘 ±3px/帧自动滚，并把落点标到露出的首/末行），Inbox / Waiting / Someday / Project 均与 Next 一致。

### 已知歧义（按保守默认处理，待用户确认）
- 无子父 Project 改类别仍会清 `show_in_next`（离开 Project 即退出 Next 展示）；若想要「保持 Next 展示」再说。
- Next 里原生 `next_action` 但带子事务的项，跨类别改类仍被禁止（防破坏层级），视为边缘情况，未改。

---

## 0.1.18（2026-07-21）

### 新增（deadline 自动归一）
- **启动/跨天自动把「具体日期=今天」改为「今日」**：应用启动时扫描所有 `deadline_type=date` 且日期为当天的事务，自动归一为 `today` 并清空具体日期；同时每天 0 点再次扫描，无需手动改。

### 修复（拖拽落点精确高亮）
- **空白缝隙/列表边缘始终有落点高亮**：改写 `useListDrag` 与 `NextView` 的命中检测，光标落在两条事务之间的缝隙或列表边缘时，自动吸附到最近行，并在该行上半/下半显示绿线，不再「跳到最底部」。
- **全视图拖拽手感对齐 Next**：Inbox / Waiting / Someday / Project 全部加上与 NextView 同款的拖拽浮层（气泡）、220ms 长按阈值、10px 取消阈值、光标上半/下半插入方向。

### 修复（跨类别拖拽规则与 bug）
- **已完成事务不可拖动**：任何列表中 `status=completed` 的事务长按不再进入拖拽状态，避免把已完成事项拖到 Project 后仍保持完成态。
- **无子父 Project 可改类别**：Project 视图中没有子事务的父项目，现在可以长按拖到左侧导航栏改类别；含子父项目仍只能排序。
- **Inbox 导航项不高亮**：从四类视图（Next/Project/Waiting/Someday）拖动时，左侧 Inbox 导航项不再显示可拖入高亮，直观表明禁止互转。
- **修复从时段拖到 Project 后未分配时段出现重复项**：`NextView` 的 `pointerup` 早先在某些分支里提前 `return` 导致 `cleanup` 未执行，拖动状态残留；已重构为统一收尾，并补清 `show_in_next=0`，消除跨类别后的残留显示。
- **离开 Next 时强制清空 show_in_next**：所有跨类别改类操作（Next/Project/Waiting/Someday 互转）统一把 `show_in_next` 置 0、`time_slot` 置 `none`、并脱离父项目。

---

## 0.1.17（2026-07-21）

### 新增（跨类别拖拽——重分类）
- **拖到左侧导航栏即改类别**：Waiting / Someday / Project 三个视图现已支持「长按事务 → 拖到左侧导航项」直接改类别（沿用 Next 视图已有的跨类别拖拽）。悬停导航项时该项高亮（accent 描边 + 加粗）。
- **Inbox ↔ 四类禁止互转**：拖到 Inbox 导航项一律无操作（inbox 是收集桶，不能与正式类别互相转换）。
- **含子父项不可改类别**：Project 视图里带子事务的父项只能列表内拖动排序，不能拖去改类别、也不能改父（避免破坏层级）。

### 新增（Project 改父）
- **子事务拖到父行即改父**：Project 视图里把子事务拖到某个父事务行上，即成为该父的子事务（落点父行高亮 accent 内填充）；含子父项与自身不可作为改父目标。子事务仍可在同级间拖动排序。

### 规则细化（Next 上下文）
- **显示在 Next 的外来项不能从 Next 改类别**：project 子事务 / waiting / someday 中「加入 Next」展示在 Next 的事务，在 Next 视图里只能排时段/排序，拖到导航栏改类别一律无效（只能在它们自己所属的 Project / Waiting / Someday 视图里改类别）。
- **离开 Next 上下文重置 time_slot**：在任意视图把事务拖去其它类别时，一并清空 `time_slot`（时段是 Next 概念），并解除 `parent_id`。

---

## 0.1.16（2026-07-20）

### 新增（数据模型）
- **`time_slot` 字段**：SQLite 新增 `time_slot TEXT NOT NULL DEFAULT 'none'`（向后兼容迁移），贯通 Rust 结构体 / 命令 / TS 类型 / 导入导出。取值 `none | morning | noon | evening`，用于把 Next 事务分配到一天的三个时段。

### 新增（Projects 视图）
- **子事务可收起**：每个父事务下的子事务列表**默认收起**，点击父标题旁箭头或父行可切换展开 / 收起；收起后只显示父事务与完成度，列表更清爽。
- **「加子步骤」改为「批量添加子事项」**：点击后弹出子事务编辑窗，提交一个即清空表单、原地弹出下一个，连续生成多个子事项；点取消 / × / Esc / 遮罩即结束批量。参考 Inbox「批量转换」的连续弹窗模式。
- **子事务编辑去掉类型选项**：Project 子事务的编辑弹窗不再出现「选择类型」分段（子事务类型恒为 next_action，原选项无意义）。
- **子列表布局修复**：原父行右侧被「子排序 / 编辑 / 加子步骤」三按钮占满，子事务过多时右侧留白。改为**父项上方工具栏（排序 + 编辑 + 批量添加 + 完成度）换行排列**，子事务列表拉满整行宽度、不再右侧留空。

### 新增（备注交互）
- **所有备注默认一行收起**：Inbox / Next / Waiting / Someday / Project（父与子）共五处列表的备注均改为默认单行截断（最多两行），点击在「一行 ↔ 完整」间切换；**点击其它地方由全局监听自动收起**，一次只展开一条。

### 新增（日期检测）
- **「具体日期」= 今天自动归一**：在编辑 / 新增 / 整理 / 加子事项四种弹窗中，若「时间要求」选了「具体日期」且所选日期正好是今天，保存时**自动改为「今日」并清空具体日期**，统一按 today 处理（避免「今天」被当成未来某天），即时落库。

### 新增（Next Actions 分栏）
- **左右分栏**：Next 视图改为左半 + 右半各占一半。左栏是**未分配时段的 Next 事务**（原 Next 子页面语义）；右栏分出**早时段 / 午时段 / 晚时段**三个子栏。长按左栏事务拖入右栏某时段即分配（持久化到 `time_slot`，拖回左栏则取消分配），据此规划一天三个时段要完成的任务。

### 新增（启动弹窗）
- **每次启动弹「今天」**：软件启动时自动弹出，显示今天日期（年/月/日 + 星期），并按 `time_slot` 分别列出已分配到早 / 午 / 晚 三个时段的未完成任务。关闭后进入主界面（开发刷新也会弹）。

---

## 0.1.15（2026-07-20）

### 发布说明
- **固化 0.1.14 开发期全部修复**：0.1.14 期间已完成并验证通过的功能与修复——子事务/无子父事务移动 root-cause 修复（`clear_parent` 标志）、清空回收站 `FOREIGN KEY constraint failed` 安全网（先提拔活子项为顶层再删父）、SQLite 写锁 `PRAGMA busy_timeout`、Mutex 毒化容错、父事务含子禁止移动、各类别「按完成情况」排序、Project 父事项改「按完成度」排序、TrashModal 真实报错暴露——在本版本正式定版，版本号由 0.1.14 升至 0.1.15。**无新增功能，仅做版本固化与变更记录归档**。完整改动清单见下方 0.1.14 章节。

## 0.1.14（2026-07-20）

### 新增
- **父事务含子事务禁止移动**：Project 视图多选模式下，若选中的父事务带有子事务，点击「移动到…」时不再执行移动，并在工具条内提示「含子事务无法移动」（约 2.5 秒后自动消失）。避免把父项移走后子事务成为孤儿、破坏层级。
  - 细化：仅跳过「含子事务的父事务」，其余选中项（**子事务、无子父事务**）照常移动到其他类别；若整批都不可移动则只提示不改动。
- **各类别排序新增「按完成情况」**：Next / Project / Waiting / Someday 四类列表的排序下拉，与 Project 子事务排序一致，新增「按完成情况」——未完成项前置、已完成项后置（同状态回退原顺序）。
  - 细化（Project 父事项排序）：原本按 `status` 排序无效（父项完成态由子事务派生、库内不一定是 completed，导致"没反应"）。改为按**完成度**排序——有子事务则取"已完成子事务占比(0~1)"，无子事务取自身是否完成；**完成度低的排在前**。

### 修复
- **子事务 / 无子父事务移动失效（根因修复）**：`moveTo` 逻辑此前已正确（仅跳过含子父事务、其余照常移动），但前端传 `parent_id: null` 想让子事务脱离父事务时，Rust 端 `PatchInput.parent_id: Option<i64>` 的 `if let Some(v)=patch.parent_id` 把 `null` 当 `None`（"不修改"）直接跳过，子事务的 `parent_id` 永远清不掉——移动后既不在目标类显示、又仍挂在 Project 下，看起来"没移动"。
  - ⚠️ 中间误判：曾把 `parent_id` 改成 `Option<Option<i64>>`，以为 `null`→`Some(None)`→置空。但 **JSON 下 `Option<Option<i64>>` 的 `null` 只会落到最外层 `None`，`Some(None)` 不可达**，该方案对清空无效。
  - ✅ 正解：新增独立布尔标志 `clear_parent: Option<bool>`（Rust `PatchInput` 加字段；handler 中 `if let Some(true)=patch.clear_parent { add!("parent_id", Option::<i64>::None) } else if let Some(v)=patch.parent_id { add!(...) }`）。前端 `moveTo`（ProjectListView 与 CategoryListView）改为 `updateTx(id, { category: target, clear_parent: true })`。`update`/`updateTx` 的类型签名也放开 `clear_parent?: boolean`。重编译后子事务与无子父事务均可正常脱离 Project 并出现在目标类别。
- **SQLite 写锁（清空回收站等偶发失败的根因）**：dev 与 release `NowTree.exe` 共享同一 `nowtree.sqlite`（同 identifier）。rusqlite 默认无 busy 超时，双开并发写会立刻抛 `database is locked`，导致清空回收站 / 移动 / 软删偶尔"点了没反应"。已在 `src-tauri/src/db.rs` 的 `Db::new` 打开连接后执行 `PRAGMA busy_timeout = 3000;`，并发写改为等待而非立即报错，覆盖全部写路径。
- **Mutex 毒化导致全部 DB 命令连锁失败**：所有 Tauri 命令原用 `state.0.lock().unwrap()`。一旦任一命令在持锁期间 panic（如某条特殊数据触发 `row.get().unwrap()` 越界），`Mutex` 即被毒化，之后含 `empty_trash` 在内的所有命令都会 panic 失败——这正是"清空/移动偶发不行"的典型表现。已将全部 12 处 `lock().unwrap()` 改为 `lock().unwrap_or_else(|e| e.into_inner())`（毒化后自动接管内部连接，不中断后续命令）。
- **清空回收站 `FOREIGN KEY constraint failed`（已修复，根因）**：Tauri 进程的连接里**外键约束是开启的**（Python 直连那条独立连接默认关闭，故此前没复现）。回收站里有父事务 `test`（`deleted=1`）仍带着**活着的子事务**（子项 `parent_id` 指向它），删除父项时触发外键冲突。此前"孤儿清理"只处理了"父已不存在的子项"，漏掉了"父仍在表内（仅 `deleted=1`）却带活子项"这一真场景。修复：`empty_trash` 在删 `deleted=1` **之前**，先把所有"引用即将被删父事务"的活子项 `parent_id` 置 NULL（提升为顶层），再删除父项——既解除外键引用、又保留活子项不丢失；`purge_transaction`（单条彻底删除父事务）同样先解除子项引用再删。
- **清空回收站暴露真实报错**：`TrashModal` 的清空/恢复/彻底删除动作包了 `try/catch`，**失败时把后端真实报错用红字显示在弹窗底部**（如仍失败可据此定位，不再静默"没反应"）。
- ⚠️ 操作提示：清空回收站务必**只开一个 NowTree 窗口**（dev 与 release exe 指向同一 DB，双开会争锁——已有 busy_timeout 兜底等待，但仍建议单开以确保所见即所得）。

## 0.1.13（2026-07-20）

### 修正（UI 细节）
- **左下角菜单自动关闭**：点击菜单外部区域（非菜单按钮）时，菜单自动收起。
- **Project 父事务选择框只在多选时显示**：有子事务的父事务在正常模式下左侧不再显示控件（像 Inbox 一样），仅在进入多选模式后显示 radio 选择框；且**有子事务时标题直接左对齐**，不再空出一道占位空白。**无子事务的父项仍保留手动复选框**（可单独标记完成）。
- **子事务排序选项简化**：下拉选项去掉“子·”前缀，改为「按优先度」「按时间」「按完成情况」，其中「按完成情况」将未完成子事务前置。
- **列表工具栏新增「一键清理」**：Category 列表与 Project 列表工具栏新增「一键清理」按钮；首次点击自动选中所有已完成事务并变为「确认清理？（N）」（**不再打开多选工具栏**），左侧同时出现「取消」按钮可退出确认态；再次点击批量删除。Project 视图会同时选中已完成的子事务。
- **Inbox 转换新增「父事务」方向**：转换弹窗选「Project」类型时，类型下方多出「父事务」下拉（默认「无（作为独立 Project）」，其余为现有 Project 父事务）。选中某父事务后，这条灵感会**转成该 Project 的子事务**，从而支持「灵感 → 子事务」这一新去向。
- **全套应用图标更新**：任务栏、桌面、窗口标题栏图标统一换成新的 terraria 风格树图标（多尺寸 16–256，重新生成 icon.ico）。

### 改进（Inbox 转换与批量）
- **恢复「标题预填」**：Inbox 灵感整理为正式事务时，转换弹窗**自动预填原记录的标题**（仅标题，不预填备注），少打几个字；备注仍需自行决定。
- **新增「批量转换」**：Inbox 工具栏新增与「多选」并列的「批量转换」按钮。点击后按当前顺序**依次弹出每条灵感的整理弹窗**；某一灵感转换成功即自动弹出下一条，并实时显示「转换中 n/总数」。在任一弹窗**退出（取消 / 点 × / 按 Esc / 点遮罩）则立即中断**，不再继续弹出后续灵感。

### 改进（快捷键与弹窗）
- **Esc 关闭弹窗**：编辑、新增、整理、快捷键、回收站等所有弹窗现在支持按 **Esc** 关闭（批量转换中 Esc 即中断批量）。
- **新增「快捷键」说明**：左下角菜单新增「⌨ 快捷键」项，弹窗列出当前全部快捷键（1-5 切换视图、Enter 快速新增、Esc 关闭、长按拖拽排序等）。

### 改进（数据迁移）
- **数据导出 / 导入**：左下角菜单新增「⬆ 数据导出」「⬇ 数据导入」。导出经系统文件选择器把全部事务（含回收站）存为 JSON 备份；导入读取 JSON 并 upsert（同 id 覆盖、新 id 插入），**保留父子关系**，便于换机迁移。操作结果以浮层提示。

### 改进（Projects 视图）
- **父事务子排序**：每个父事务现在可单独对其**子事务**排序（「子·按优先度」「子·按时间」），不影响其它项目，也不影响父事务之间的排序。
- **父事务完成态由子事务派生**：有子事务的父项**去掉手动复选框**（避免与派生完成冲突），其完成态改为由子事务完成度决定——**全部子事务完成则父项标记完成**（划线虚化并显示删除按钮）；无子事务的空项目仍保留手动复选框。

### 改进（开机自启动）
- **开机自启动开关**：左下角菜单新增「开机自启动」开关，勾选即把 NowTree 加入系统开机启动，取消即移除。进入应用时自动读取当前状态显示「开 / 关」。

---

## 0.1.12（2026-07-19）

### 改进（多选与工具栏交互）
- **多选可点整行选中**：进入多选后，不再必须点行首的圆点，**点击整行任意位置即可选中/取消**该条；行首圆点仍保留作状态显示。鼠标悬停整行显示手型光标与高亮边框。
- **工具栏高度固定，列表不再下移**：此前进入多选、批量功能条（batch-bar）展开后比未展开时更高，导致工具栏变高、下方列表被往下挤。现将工具栏高度按「多选展开态」预留固定值，无论是否展开列表位置都保持不动。

### 改进（悬浮加号）
- **去掉「Enter」快捷键小气泡**：鼠标移到右下角 ＋ 时不再弹出「Enter」小提示；悬停久一点仍由系统原生提示显示按钮说明文字（保留后者）。

---

## 0.1.11（2026-07-19）

### 改进（列表工具栏与多选交互）
- **多选选中框改为圆形**：进入多选后行首出现的选中控件由方形勾选框改为**圆形选中**（Radio 风格，选中时圆环内一个实心圆点），与「完成」用的方形勾选框在视觉上明确区分，不再混淆。
- **批量功能分组框**：进入多选后新出现的「全选 / 取消 / 移动到… / 删除」被一个虚线边框框成一组，与「排序 / 多选」主按钮分离，工具栏不再显得凌乱。
- **排序与多选按钮等宽**：两个主按钮统一最小宽度，不再一长一短。
- **Inbox 去掉排序按钮**：灵感没有时间要求与优先度，排序无意义，故 Inbox 视图不再显示排序（仍可长按手动拖拽）。

### 改进（排序规则）
- **「按时间」改为按时间要求的最晚截止时刻排序**：本周取周日、本月取月底、今日取当天、具体日期取当天，统一按当日 23:59:59 比较，**最靠近现在的排在前面**；无时间要求的排到最后。排完仍可手动拖拽微调（一次性整理，不锁定模式）。

### 改进（左下角菜单）
- **菜单悬浮置顶**：左下角「☰ 菜单」改为 fixed 悬浮，浮在左右两栏之上、始终完整可见；不再被侧栏滚动裁剪、也不会在侧栏撑出横向滚动条。侧栏补 `overflow-x: hidden`。
- 修复「跟随系统」按钮文字换行（菜单可自适应内容宽度）。

### 其他
- 分类「Waiting」显示名统一改为 **「Waiting for」**（导航、标题、移动目标）。

---

## 0.1.10（2026-07-19）

### 新增（列表多选 + 批量 + 排序工具栏）
- **多选模式**：每个视图（Inbox / Next / Waiting / Someday / Project）新增「多选」按钮进入选择态；行首出现勾选框，可逐项勾选 / 「全选」/「取消」。
- **批量删除**：多选态下「删除（N）」按钮，对所选全部软删除（进回收站）。
- **批量移动（四大类互转）**：Next / Waiting / Someday / Project 多选态下出现「移动到…」下拉，可把所选事务在 next_action / project / waiting / somedy 之间互转（移动时清除 parent_id 脱离原项目树）。**Inbox 只有删除、不提供移动**（灵感不能直接批量转正式事务，需走「整理」）。
- **列表排序工具栏**：每个视图顶部新增排序下拉——「手动排序」（默认，按拖拽 order_index）/「按优先度」（高优先在前）/「按时间」（新在前）。非手动排序时自动禁用长按拖拽，避免冲突。

### 新增（回收站 + 左下角下拉 + 主题）
- **回收站**：Rust 新增 `list_deleted` / `restore_transaction` / `purge_transaction` / `empty_trash` 四个命令（已注册）。界面左下角「☰ 菜单」→「回收站」打开弹窗，列出已软删除事务，可「恢复」回原列表、或「彻底删除」从库物理移除（释放磁盘空间，不可恢复）；「清空回收站」一键彻底清空。
- **左下角下拉框**：替换原版本文字，点开含三项——回收站、主题（深色 / 浅色 / 跟随系统，写入 localStorage 持久化，跟随系统用 `prefers-color-scheme` 实时监听）、版本信息（v0.1.10 · 本地 SQLite）。
- **主题切换**：`:root[data-theme="light"]` 新增浅色变量；默认「跟随系统」。

### 修复（Inbox 编辑）
- **灵感（Inbox 条目）现在可编辑**：新增「编辑」入口，打开 EditModal 的 `inbox` 模式——仅改标题与备注，不改类别与状态（避免把灵感误转成正式事务）；删除走 `removeInbox`。

### 已知限制
- 批量移动到 Project 会使其成为顶层项目（脱离原父项目），若原是子事务则会离开原项目树；属预期简化行为。
- 回收站「彻底删除 / 清空」为物理删除，不可恢复，操作前请确认。

---

## 0.1.9（2026-07-19）

### 优化（交互手感 + 布局）
- **拖拽改为「长按整条」**：去掉每行左端的「⠿」手柄，直接**长按整条（约 220ms）**即可拖动排序，观感更干净。长按未触发前若移动超过阈值（视为滚动/选择文字）会自动取消；在勾选框、按钮、输入框、下拉等交互元素上按下不会误触发拖动；Project 子事务拖动通过 `stopPropagation` 避免冒泡到父项目。进入拖动后光标切 `grabbing`，落点仍以顶部高亮线提示。
- **右侧新增自适应「子界面」容器**：大标题与副标题固定在内容区顶部，其下方新增一个 `.sub-panel` 卡片容器，**自适应占满**剩余高度，事务/灵感列表装在其中，**滚动条收进该子界面内部**（内容区本身不再整体滚动）。Inbox / Next / Waiting / Someday / Project 五个视图统一改造。

### 已知限制
- 拖动顶层项目时若悬停在其他项目的子事务区域，落点命中可能落到子事务行（顶层与子事务共用同一命中标记，沿用 0.1.8 行为），一般不影响最终排序结果。

---

## 0.1.8（2026-07-19）

### 优化（快捷键 + 拖拽重写）
- **拖拽重写为 Pointer Events 手拖**：0.1.7 的 HTML5 原生 `draggable` 在 Tauri WebView2 里触发"禁止"光标、无法拖起。改为基于 `pointerdown/move/up` 的指针拖拽（新增 `src/hooks/useListDrag.ts`），不再依赖浏览器原生 DnD，从根上消除"禁止"手势，跨端表现一致。每行左端新增拖拽手柄「⠿」（仅手柄可拖），落点以顶部高亮线提示。Inbox / Next / Waiting / Someday / Project（项目顶层 + 子事务）全覆盖；排序仍写入 `order_index` 持久化，刷新保持。
- **快捷键 1-5 切换视图**：`1`=Inbox、`2`=Next、`3`=Project、`4`=Waiting、`5`=Someday；在输入框/下拉/弹窗内输入时不触发（避免误切）。
- **回车打开加号（新增）**：非输入态且无可关闭弹窗时按 `Enter`，向当前视图派发 `nowtree:quick-add` 事件，自动弹出对应类别的新增弹窗；各视图监听该事件打开 FAB 弹窗。
- **FAB 悬停显示快捷键**：右下角加号按钮旁新增 `Enter` 提示气泡，鼠标移上去即显示（新增 `src/components/Fab.tsx` 封装按钮 + 提示）。
- **注意**：全局快捷键在 `src/App.tsx` 注册；若焦点在侧边栏 `a` 链接上按回车会同时取消锚点跳转并触发新增，属预期。

### 修复
- 0.1.7 拖拽在 WebView 内"按住即禁止"的问题（原生 DnD 不兼容），由指针事件方案彻底解决。

---

## 0.1.7（2026-07-19）

### 优化
- **Inbox 输入框下线，统一用加号**：移除 Inbox 顶部「灵感输入栏」（标题+备注+记录按钮），改为点击右下角悬浮 ＋ 直接新增记录，与其它界面交互一致。
- **列表项支持手动拖拽排序**：Inbox / Next / Waiting / Someday / Project（含项目顶层与子事务）的每条事务均可拖动重排，松手即按新顺序持久化（写入 `order_index` 到本地 SQLite）；未手动排过的项按创建时间兜底排序。拖动时该项半透明、光标变 grabbing 提示。

### 已知限制
- 拖拽排序为「单视图内」重排：在某视图拖动的顺序只影响该视图展示，不同视图（如 Inbox 与 Next）各自维护自己的顺序，互不串扰。

### 新增
- **跳过 Inbox 直接新增**：每个界面右下角新增圆形悬浮加号按钮（FAB），点击即在当前界面直接新增对应类别的事务。
  - 弹窗标题写明「新增 XX 事务」（如「新增 Next Action 事务」「新增 Project 事务」「新增 Inbox 记录」），**不再出现类别选择板块**，类别由当前界面固定决定。
  - Inbox 悬浮新增仅含标题 + 备注；其它类别含优先级 / 时间要求 / 提醒，字段与编辑弹窗一致。

### 优化
- **Project 事务健康度**：项目下新增子事务后，父事务节点展示一条「事务健康度 x/y 完成」进度条（y = 子事务总数，x = 已完成数），完成越多进度条越长，醒目直观。
- **Inbox 整理弹窗改版**：整理为正式事务时，**不再自动预填原记录的标题与备注**（输入框留空，原记录以只读文字「原记录：…」展示供参照）；并将「类型」区块移到「备注」上方（类型在上、备注在下）。

### 已知限制
- 提醒日期选择器里的「今天」按钮属于系统（Chromium/Edge）原生控件，无法通过应用层 CSS/JS 移除；如需彻底去掉需后续替换为自定义日历组件。

### 优化
- **字段顺序调整**：编辑 / Inbox 整理 / Project 加子步骤弹窗中，「时间要求」区块移到「提醒」上方，符合"先定 Deadline、再设提醒"的填写习惯。
- **Next 来源显示简化**：来自 Project 子步骤的事务在 Next 列表中统一显示「来源：project」，不再带具体项目名称；Waiting / Someday 来源仍显示为「来源：Waiting」「来源：Someday」。

### 说明
- **关于提醒日期选择器里的「今天」按钮**：该按钮是 Chromium/Edge 原生日期选择器内部的控件，并非 NowTree 自身 UI，目前无法通过 CSS/JS 移除。若希望完全自定义，需要后续替换为手写日历组件。

### 已知限制
- 提醒弹窗依赖 NowTree 正在运行（App 内 30 秒轮询），关闭程序后到期提醒不会触发，重新打开才会补弹。
- 首次运行会请求系统通知权限，需点击「允许」。

---

## 0.1.4（2026-07-19）

### 优化
- **图标替换为压缩包中的圆润像素树**：从 `temp/terraria_tree_v2_pack.zip` 提取 32×32 源图，最近邻放大生成 128×128、256×256（@2x）与 `icon.ico`。
- **优先级默认 1，去掉「无」选项**：编辑/整理弹窗中的优先级选择只保留 1-5 色块，默认 1，选中态用对应颜色边框/底色高亮，不再使用主题色。
- **DDL 过期警示**：时间要求设置后（今日/本周/本月/具体日期），若当前时间超过该 DDL 截止时间（如本周日 24:00），原时间标签变为 `⚠ 注意：未完成`。
- **Project 子步骤创建弹窗**：点击「加子步骤」打开完整弹窗，支持标题、备注、优先级、提醒、时间要求，与编辑弹窗字段一致。
- **Next 视图显示子事务来源**：来自 Project 子事务的项显示 `来源：xxx项目`；来自 Waiting/Someday 的项显示 `来源：Waiting` / `来源：Someday`。
- **提醒选择器自动关闭**：选择时间后调用 `blur()`，让原生选择器自动关闭。
- **具体日期选择器更简洁**：选择「具体日期」后仅显示所选日期和「清除」按钮；未选时不再显示「选择日期」占位文字；清除后类别复位为「无」。

### 修复
- **修复提醒日历图标偶发点不动**：改为真实 📅 按钮 + 用户手势内直接 `showPicker()`，避免透明覆盖层状态不稳的问题。
- **修复具体日期清除后再选无反应**：清除时同时把 deadline_type 复位为 none，使再次选择「具体日期」能触发 onChange。
- **精简输入标签**：标题、备注占位提示只保留「标题」「备注」二字，无括号及额外说明。

### 已知限制
- 提醒弹窗依赖 NowTree 正在运行（App 内 30 秒轮询），关闭程序后到期提醒不会触发，重新打开才会补弹。
- 首次运行会请求系统通知权限，需点击「允许」。

---

## 0.1.3（2026-07-19）

### 修复
- **时间要求「具体日期」不再出现日历图标**：选择具体日期后只显示所选日期与「清除」按钮，不常驻日历按钮；点击日期文本可再次打开选择器。
- **修复「具体日期」清除后再选无反应**：原清除只清日期、没动类别，`<select>` 值仍是「具体日期」，再选同一项不触发 `onChange`。现在清除时把类别复位为「无」，再次选「具体日期」即可正常弹出选择器。
- **修复提醒日历图标点击偶发无响应**：原实现用透明 `datetime-local` 叠在图标上靠"原生点击"，清除后元素状态不稳，需移开或等待才响应。改为真实 📅 按钮 + 在用户手势内调用 `showPicker()`，每次点击都稳定弹出。
- **输入标签精简**：标题、备注的提示从「标题重填」「备注 Ctrl+Enter 换行，Enter 保存」改为仅「标题」「备注」（键盘行为不变：标题 Enter 保存，备注 Ctrl/Shift+Enter 换行、Enter 保存）。

### 已知限制
- 提醒弹窗依赖 NowTree 正在运行（App 内 30 秒轮询），关闭程序后到期提醒不会触发，重新打开才会补弹。
- 首次运行会请求系统通知权限，需点击「允许」。

---

## 0.1.2（2026-07-19）

### 修复
- **修复整理转换后事务重复的 bug**：`loadActive` 已加载全部未删除事务（含 Inbox），`convertInbox` 不再重复追加，而是原地替换/追加，避免同一 `id` 在列表中出现两次。
- **修复完成勾选后"孪生事务"同步变完成的 bug**：由上述重复项导致，修复后勾选、取消、删除均按唯一 `id` 生效。
- **修复同标题事务勾选互相影响**：同一 ID 的重复渲染导致勾选状态看起来联动，随重复项修复一并解决。
- **修复时间要求下拉显示重复括号**：例如「本周（本周（7.13-7.19））」改为「本周（7.13-7.19）」，具体日期直接显示为 `2026-07-31`。
- **修复具体日期选择换行**：选择「具体日期」后，日期选择器、已选日期、清除按钮一行展示。
- **Inbox 整理弹窗增加提醒字段**：与编辑弹窗一致，可在整理阶段就设置提醒时间（后端 `convert_inbox` 同步支持）。
- **修复备注键盘方向**：此前实现反了（Ctrl+Enter 保存、Enter 换行），改为 Ctrl/Shift+Enter 换行、普通 Enter 保存。

---

## 0.1.1（2026-07-19）

### 优化
- **优先级只上色、不写数字**：去掉 `P1..P5` 文本，改为整行左侧色条 + 淡底色（5=红最急 → 1=蓝最低，默认无不上色）。
- **完成 = 与「编辑」并列的勾选框，且不消失**：每条事务（含 Project 子事务）勾选后加删除线 + 半透明虚化，保留在原处；再点取消恢复。Project 子事务与全局 Next 因同一条记录自动同步。
- **Wait / Someday 也支持「加入 Next」**：Waiting、Someday 列表项可切换到全局 Next。
- **编辑窗回车保存**：标题 Enter 保存；备注 Ctrl/Shift+Enter 换行、Enter 保存。
- **提醒到点桌面弹窗**：编辑弹窗增加「提醒」时间选择，依赖 App 运行（30 秒轮询）触发系统通知。
- **右侧内容区独立滚动**：左侧边栏固定，仅右侧内容滚动，所有页面统一。
- **重绘圆润像素树图标**：椭圆/圆形树冠，格子更精细。
- **版本号策略**：保持 0.1.x 开发期迭代，稳定后再升 1.0.0。

---

## 0.1.0（2026-07-19）

NowTree 第一阶段 MVP：个人 GTD 事务管理系统（React + TypeScript + Tauri/Rust + SQLite）。

### 功能
- **Inbox 快速收集**：输入标题/备注，一键丢入 Inbox，稍后整理。
- **整理转换**：Inbox 中的想法可转为 Next Action / Project / Waiting / Someday 四类正式事务，原地改写不重复建记录。
- **四类视图**：Next Action、Waiting、Someday 列表，Project 专用树状视图。
- **Project 树与子事务**：Project 下可拆分子事务，子事务可手动「加入 Next」进入全局 Next 列表。
- **统一编辑弹窗**：编辑标题、备注、类型、时间要求、优先级、提醒，支持软删除。
- **完成态**：勾选完成，事务保留在列表中，加删除线与虚化效果，不消失。
- **完成后快捷删除**：已完成事务显示「删除」按钮，二次确认后软删除归档。
- **优先级可视化**：1-5 优先级映射为整行左侧色条 + 淡底色。
- **时间要求**：无 / 今日 / 本周 / 本月 / 具体日期，选项带具体日期提示。
- **本地持久化**：SQLite 存储，Rust/Tauri 后端，数据关闭后重开仍在。
- **启动脚本**：提供 `run-dev.bat`（开发调试）与 `build-release.bat`（发布打包），自动配置非标准 MSVC/SDK 环境变量。

### 已知限制
- 需要本机安装 Rust、MSVC 构建工具、Windows SDK、WebView2 才能编译运行。
- 打包正式安装包（`.msi`/`.exe` 安装器）需额外安装 WiX 或 NSIS；默认仅输出可直接双击的 `NowTree.exe`。
